local keys = dofile("utils.rte/Data/Keys.lua")

return {
	show_modmod = keys.N,
	up = keys.ArrowUp,
	down = keys.ArrowDown,
	left = keys.ArrowLeft,
	right = keys.ArrowRight,
	backspace = keys.Backspace,
	enter = keys.Enter,
}
